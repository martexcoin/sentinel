# specify path to marte.conf or leave blank
# default is the same as MarteX Core
martex_conf=/root/.MXT/MarteX.conf

# valid options are mainnet, testnet (default=mainnet)
#network=mainnet
network=testnet

# database connection details
db_name=database/sentinel.db
db_driver=sqlite
